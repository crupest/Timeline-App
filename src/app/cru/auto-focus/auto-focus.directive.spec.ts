import { CruAutoFocusDirective } from './auto-focus.directive';

describe('AutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CruAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
